Simple Python program to read temperature from DS18B20 temperature sensors attached to a Raspberry PI.

First a list of attached sensors is acquired, then it loops through the sensors getting the raw temp and converting to degrees Celsius to print out to screen.
